version: '2'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      - RAILS_ENV=staging
  memcached:
    image: memcached:latest
networks:
  default:
    external:
      name: sharetribe
